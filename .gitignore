# Deployment files (keep only on server, not in repo)
deploy.php
deploy.log
.log

# Environment files
.env
.env.local
.htaccess
.env.example

# Static config — secrets must come from js/config.php (reads .env server-side)
# Committing this file would expose API keys. It is safe to delete any local copy.
js/config.js

# IDE/Editor
.idea/
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db
